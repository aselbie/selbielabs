{
  "name": "selbielabs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf dist && brunch build -d -p && hugo",
    "start": "brunch watch -d & hugo server --buildDrafts"
  },
  "author": "",
  "license": "ISC",
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "browsers": ["last 2 versions", "safari >= 7"]
          }
        }
      ]
    ]
  },
  "devDependencies": {
    "autoprefixer": "^7.1.2",
    "babel-brunch": "^6.1.1",
    "babel-preset-env": "^1.6.0",
    "brunch": "^2.10.9",
    "clean-css-brunch": "^2.10.0",
    "postcss-brunch": "^2.0.5",
    "rimraf": "^2.6.1",
    "sass-brunch": "^2.10.4",
    "uglify-js-brunch": "^2.10.0"
  }
}
